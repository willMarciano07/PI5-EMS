<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:usesCleartextTraffic="true"
        android:enableOnBackInvokedCallback="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EMS"
        tools:targetApi="33">
        <activity
            android:name="com.example.ems.MainActivity"
            android:exported="false" />

        <activity
            android:name="com.example.ems.ProdutosActivity"
            android:exported="false" />

        <activity
            android:name="com.example.ems.EditarProdutoActivity"
            android:exported="false" />

        <activity
            android:name="com.example.ems.IncluirProdutoActivity"
            android:exported="false" />

        <activity
            android:name="com.example.ems.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>